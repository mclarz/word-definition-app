let getters = {

    search: state => {
        return state.search
    },
    results : state => {
        return state.results
    },
    histories: state => {
        return state.histories
    }
}

export default getters;